import type { Registry, TypeDef } from '@polkadot/types/types';
import type { ParamDef } from '../types';
declare const _default: (registry: Registry, type: TypeDef) => ParamDef[];
export default _default;
